import React from 'react';

import NotFoundPageView from './notFoundPage.view';

const NotFoundPageContainer = (): React.ReactElement => {
  return <NotFoundPageView />;
};

export default NotFoundPageContainer;
